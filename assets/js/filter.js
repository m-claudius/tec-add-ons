(function($){function readSelected(){var s=[];$('.tec-filter-bar .tec-filter-checkbox:checked').each(function(){s.push($(this).val());});return s;}$(document).on('click','.tec-filter-apply',function(e){e.preventDefault();var s=readSelected();var $f=$(this).closest('.tec-filter-bar').find('form.tec-filter-form');$f.find('input.tec-cat-hidden').val(s.join(','));$f.trigger('submit');});$(document).on('click','.tec-filter-clear',function(e){e.preventDefault();var $b=$(this).closest('.tec-filter-bar');$b.find('.tec-filter-checkbox').prop('checked',false);var $f=$b.find('form.tec-filter-form');$f.find('input.tec-cat-hidden').val('');$f.trigger('submit');});})(jQuery);